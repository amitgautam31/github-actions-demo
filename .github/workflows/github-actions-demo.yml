name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: push
# on: 
#   schedule:
#     - cron: '*/10 * * * *'
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9' # install the python version needed          
      - name: Check out repository code & install packages
        run: |
          git clone https://github.com/xataio/xtools.git
          cd xtools/xreplay
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python xreplay.py \
            --from_workspace Amit-s-workspace-1ige91 \
            --from_database dev \
            --from_branch main \
            --from_region ap-southeast-2 \
            --from_XATA_API_KEY $SOURCE_XATA_API_KEY \
            # --output file
            --to_workspace Amit-s-workspace-1ige91 \
            --to_database dev \
            --to_branch daily \
            --to_region ap-southeast-2 \
            --to_XATA_API_KEY $DESTINATION_XATA_API_KEY \
            --output xata
          ls
        env:
          SOURCE_XATA_API_KEY: ${{ secrets.SOURCE_XATA_API_KEY }}
          DESTINATION_XATA_API_KEY: ${{ secrets.DESTINATION_XATA_API_KEY }}
      # - name: commit files
      #   run: |
      #     git config --local user.email "action@github.com"
      #     git config --local user.name "GitHub Action"
      #     git add -A
      #     git diff-index --quiet HEAD || (git commit -a -m "updated logs" --allow-empty)
      # - name: push changes
      #   uses: ad-m/github-push-action@v0.6.0
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     branch: ${{ github.workspace }}
      - name: cd the content here
        run: ls
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."      
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
